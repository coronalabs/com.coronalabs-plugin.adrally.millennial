Sources for the plugin `plugin.adrally.millennial`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.adrally.millennial" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
